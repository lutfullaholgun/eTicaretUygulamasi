
@model IEnumerable<AppUser>

@{
    ViewData["Title"] = "User Management";
}

<div class="container-fluid px-lg-5">
    <!-- Container Start -->
    <div class="card shadow-lg border-0 rounded-lg mt-4">
        <div class="card-header bg-primary text-white py-3">
            <div class="row align-users-center justify-content-between">
                <div class="col-12 col-md-6">
                    <h3 class="mb-0">@ViewData["Title"]</h3>
                </div>
                <div class="col-12 col-md-6 text-end">
                    <a asp-action="Create" class="btn btn-success">
                        <i class="fas fa-user-plus"></i>
                        <span class="d-none d-md-inline">Add New User</span>
                    </a>
                </div>
            </div>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-striped table-hover align-middle mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.Name)</th>
                            <th class="d-none d-lg-table-cell">@Html.DisplayNameFor(model => model.SurName)</th>
                            <th>@Html.DisplayNameFor(model => model.Email)</th>
                            <th>Status</th>
                            <th class="d-none d-xl-table-cell">@Html.DisplayNameFor(model => model.CreatedDate)</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modeluser => user.Name)</td>
                                <td class="d-none d-lg-table-cell">@Html.DisplayFor(modeluser => user.SurName)</td>
                                <td>@Html.DisplayFor(modeluser => user.Email)</td>

                                <td>
                                    @if (user.IsActive)
                                    {
                                        <span class="badge bg-success">Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Inactive</span>
                                    }
                                    @if (user.IsAdmin)
                                    {
                                        <span class="badge bg-warning text-dark mt-1 d-block d-md-inline">Admin</span>
                                    }
                                </td>
                                <td class="d-none d-xl-table-cell">@Html.DisplayFor(modeluser => user.CreatedDate)</td>
                                <td>
                                    <div class="d-flex gap-1 flex-wrap">
                                        <a asp-action="Update" asp-route-id="@user.Id"
                                           class="btn btn-sm btn-primary"
                                           title="Update"
                                           data-bs-toggle="tooltip">
                                            <i class="fas fa-edit"></i>
                                            @* <span class="d-none d-lg-inline">Edit</span> *@
                                        </a>
                                        <a asp-action="Details" asp-route-id="@user.Id"
                                           class="btn btn-sm btn-info"
                                           title="Details"
                                           data-bs-toggle="tooltip">
                                            <i class="fas fa-eye"></i>
                                            @* <span class="d-none d-lg-inline">Details</span> *@
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@user.Id"
                                           class="btn btn-sm btn-danger"
                                           title="Delete"
                                           data-bs-toggle="tooltip">
                                            <i class="fas fa-trash-alt"></i>
                                            @* <span class="d-none d-lg-inline">Delete</span> *@
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer bg-light py-3">
            <div class="row">
                <div class="col-12 col-md-6">
                    <p class="mb-0 text-muted">Total Users: @Model.Count()</p>
                </div>
            </div>
        </div>
    </div>
</div>

